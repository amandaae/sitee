/* CSS */
.navbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 10px 20px;
    background-color: #333;
    color: #fff;
}

.navbar-menu {
    display: none;
    list-style-type: none;
    padding: 0;
    margin: 0;
}

.navbar-menu li {
    padding: 10px 0;
}

.navbar-burger {
    cursor: pointer;
    padding: 10px;
}

.bar {
    display: block;
    width: 25px;
    height: 3px;
    background-color: #fff;
    margin: 5px 0;
}

#navbar-toggle:checked + .navbar-burger .bar {
    transform: rotate(45deg) translate(5px, 5px);
}

#navbar-toggle:checked + .navbar-burger .bar:nth-child(2) {
    opacity: 0;
}

#navbar-toggle:checked + .navbar-burger .bar:last-child {
    transform: rotate(-45deg) translate(5px, -5px);
}

#navbar-toggle:checked ~ .navbar-menu {
    display: block;
}
